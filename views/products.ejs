<html>
    <head>
        <link rel="stylesheet" href="/styles.css">
        <title>Products</title>
    </head>
    <body>
        <h1>Products page</h1>
        <%= message %>

        <%- include('navbar', {links: navLinks}) %>

        <ul>
            <% users.forEach((user) => { %>
                <%- include('users/user', {user: user}); %>
            <% }); %>
        </ul>

        
        <%- include('footer', {links: footerLinks}) %>
    </body>
</html>